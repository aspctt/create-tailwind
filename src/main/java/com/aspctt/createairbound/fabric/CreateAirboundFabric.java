package com.aspctt.createairbound.fabric;

import net.fabricmc.api.ModInitializer;

// The Fabric entry point, and the only class the Fabric target compiles. Everything else is written against
// NeoForge and Create's NeoForge build, and Create's Fabric port has no 1.21.1 build to port it to yet.
public class CreateAirboundFabric implements ModInitializer {
    @Override
    public void onInitialize() {
    }
}
